# Plan: spore:ADR-0072 — /end Skill Validator-Check Integration

**NOTE**: This plan was intended at `/Users/darrenzal/.claude/plans/adr-0072-end-skill-validator-check-integration.md` per Section 8 deliverable spec. The child agent's Write + Bash access to `~/.claude/plans/` was denied by sandbox. Orchestrator or operator should either (a) copy this file to the canonical plans location after Step 2 approval, or (b) treat this tmp/-hosted plan as the authoritative plan for this ADR (matching ADR-0071's session-atomic pattern where plan-documentation can live in tmp).

---

**Parking source**: spore:ADR-0067 Axis B5 deferral (validator-check integration in /end skill)
**Parent parking source**: spore:ADR-0065 §Parking lot line 341
**Canon-rebuild arc extension**: 24th canon-decision (cross-repo hygiene infrastructure)
**Date**: 2026-04-25

## Context

The 2026-04-24 ADR-0065 entry-gate validator-drift incident exposed a workflow-hygiene gap: the ruddick-2026 file carried an invalid `status: archived` value for ~18 hours before ADR-0065 preflight caught it. The drift originated in a parallel IndigenomicsAI session and was invisible until the next canon-ADR preflight ran the validator. spore:ADR-0067 closed the enum-schema side (adding `archived` as 5th DECISION_RECORD_STATUSES value). ADR-0067 Axis B5 deferred the workflow-discipline side (validator-check integration in `/end`) because the skill-infra concern involves:
- A cross-repo edit to `/Users/darrenzal/projects/darren-workflow/skills/end/SKILL.md`
- Its own baseline-mechanism sub-decisions (where does baseline live / refresh cadence / blocking-vs-warning)

ADR-0072 closes the deferred item. The intent is: invoke the canon validator as part of `/end` so drift surfaces at session-wrap, not at next-session preflight.

## Step 0 — Preflight (DONE)

Preflight manifest at `/Users/darrenzal/projects/spore/tmp/adr-0072-preflight-manifest.txt`.

**HEADs verified**:
- Spore: `0800679` ✓
- IC: `8ce665e` ✓ (read-only)
- PM: `86f2d4b` ✓ (read-only)
- darren-workflow: `021153e` ✓

**Constraint-10 tripwire**: `darren-workflow/memory/MEMORY.md` has tracked 36-line additive modification (parent-session housekeeping). Disposition required at Step 2.

**Validator baseline**: 9 errors / 30 warnings (exit 1). ADR-0072 target is hold 9/30 exact.

**ADR slot**: 0072 empty ✓.

## Step 0.5 — Audit (DONE)

Audit manifest at `/Users/darrenzal/projects/spore/tmp/adr-0072-audit-manifest-2026-04-25.md`.

Key findings:
- SKILL.md: 131 lines; 7 steps; no existing validator hook
- Validator CLI: `python3 scripts/validate_spec_dag.py`; exit 0/1; parseable `FAILED/PASSED: X errors, Y warnings` output line
- Cross-repo: only Spore has a validator; IC + PM confirmed absent
- Precedent: doc-check skill invokes `scripts/build_semantic_roadmap.py` via Bash with graceful fallback
- Natural insertion points in SKILL.md: pre-summary (after Step 2) or pre-update (after Step 4)

## Step 1 — Decision-brief

9-axis decision-brief at `/Users/darrenzal/projects/spore/tmp/adr-0072-decision-brief-2026-04-25.md`.

### Axes

- **A. ADR shape**: A1 DECISION-RECORD / A2 SPEC / A3 HYBRID
- **B. Baseline-mechanism**: B1 file / B2 no-baseline absolute / B3 git-index / B4 in-memory
- **C. Refresh cadence**: C1 per-session-start / C2 per-commit / C3 manual / C4 never (implied by B2)
- **D. Blocking-vs-warning**: D1 warning / D2 blocking / D3 configurable / D4 tiered
- **E. Scoped-diff vs full-pass**: E1 full / E2 scoped / E3 hybrid
- **F. Cross-repo scope**: F1 bundled / F2 Spore-ADR-only / F3 sequential
- **G. Global-coherence**: G1 narrow / G2 wide
- **H. Per-repo applicability**: H1 Spore-only / H2 multi-repo / H3 configurable (detect-and-run)
- **I. Missing validator handling**: I1 silent / I2 note / I3 warn

### Child recommendations (ambiguity-flagged)

| Axis | Child lean | Ambiguity |
|------|-----------|-----------|
| A | A1 DECISION-RECORD | LOW |
| B | B2 no-baseline absolute | MEDIUM — B2 conflicts with D2; if operator wants blocking, B1/B4 needed |
| C | N/A if B2; else C1 per-session-start | Dependent on B |
| D | D1 warning-only | MEDIUM — operator may prefer D4 tiered |
| E | E1 FULL-PASS | LOW |
| F | F3 SEQUENTIAL | MEDIUM — F1 bundled keeps single window; F3 mirrors ADR-0071 |
| G | G1 NARROW | LOW |
| H | H3 CONFIGURABLE (detect-and-run) | LOW |
| I | I2 NOTE | LOW |

### Axis interactions

- **B × D**: B2 + D2 = IMPOSSIBLE (validator already fails absolute; would strand every /end in Spore)
- **F × Constraint-10**: F3 sidesteps `memory/MEMORY.md` tripwire; F1 requires pre-ratification
- **H × I**: H3 + I2 = minimal behavioral footprint

### Lean bundles

**PARSIMONY-PREFERRED** (minimum-viable):
- A1 + B2 + D1 + E1 + F3 + G1 + H3 + I2

**ENFORCEMENT-PREFERRED**:
- A1 + B1 + C1 + D4 + E1 + F3 + G1 + H3 + I2

Both share A/E/F/G/H/I; tradeoff is B+C+D cluster.

## Step 2 — HANDBACK (session ends here)

Return to orchestrator. No Step 3+ execution.

## Steps 3-8 (post-ratification)

### Step 3: Plan confirmation with ratified axes
### Step 4: Re-verify baselines
### Step 5a: Spore draft ADR commit (ADR file creation per frontmatter spec)
### Step 5b (F1 only): darren-workflow SKILL.md commit
### Step 5c (F3 only): document as parked follow-on
### Step 6: Validator parity check
### Step 7: Spore active commit (draft → active)
### Step 7.5: Cross-repo read-only discipline check
### Step 8: Close-out manifest

## Acceptance Criteria (draft)

1. ADR-0072 file created with required frontmatter fields
2. `status: draft` at Step 5a
3. Per-axis decision cited in `authorized_by` field
4. Validator 9/30 parity after Step 5a draft commit
5. `status: active` at Step 7; `closed-on` field set
6. Validator 9/30 parity after Step 7 active commit
7. IC HEAD parity held (`8ce665e` unchanged)
8. PM HEAD parity held (`86f2d4b` unchanged)
9. F3: darren-workflow HEAD unchanged; SKILL.md md5 unchanged
10. F1: darren-workflow commit lands; explicit-path staging
11. Cross-repo citation recorded in ADR
12. Parking-source trace: ADR-0065 §Parking lot line 341 + ADR-0067 Axis B5
13. § Consequences documents new canon-method precedents
14. Close-out manifest written to tmp/
15. Plan closed

## /review-plan

2-round cap per known-ceiling discipline. Post Step 2 operator ratification only.

## Risks (per brief §9)

Already accounted for in decision-brief.
